#
# Show multiple messages in color
#
colors [can] :

  var [string] : red
	
	on_load [script] :
		show "red" (colors.var)
		show "blue" (blue)
		show "green" (green)
		show "white" (white)
		show "black" (black)
		show "yellow" (yellow)
